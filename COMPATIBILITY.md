# DSH 兼容记录

验证日期：2026-09-05。插件版本：2.6.0。根包与内层包使用同一份确切版本矩阵。

| DSH 版本 | Profile | 结果 |
| --- | --- | --- |
| `0.1.2-rc.1` | `web` | `compatible` |
| 未明确验证的旧 alpha 或其他版本 | — | `unknown` |

主任务已在独立 QA `127.0.0.1:3081` 和原 Web Profile `127.0.0.1:3080` 确认 Host/Client 加载。原有海洋等级 100、名字“小蓝”和座头鲸选择保留；20 种图鉴、PNG 导出与减少动态效果入口正常。

配套动画插件的原二态完整循环，与本插件的新墨鲸 96 帧播放均通过脚本及浏览器验证。新墨鲸由 24 个 ImageGen 原始姿态与 72 个明确标记的光栅补帧组成；两个 PNG 出口使用已有生图资产，个人数据与原始美术文件保留。

`compatible` 只代表上表版本的插件接口、存储和界面加载已验证；未知旧 alpha 不继承其他版本的实证。模型服务的认证及调用不包含在此结论内。详见[完整检查记录](docs/compatibility-0.1.2-rc.1.md)。
