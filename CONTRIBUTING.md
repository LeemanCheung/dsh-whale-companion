# Contributing

Use Node.js 22.19 or newer and Corepack. Run `corepack pnpm install`, `corepack pnpm typecheck`, `corepack pnpm test`, `corepack pnpm build`, and `corepack pnpm pack:check` before opening a pull request. Do not commit credentials, local DSH state, or generated tarballs.
